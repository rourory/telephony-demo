declare type BackendSettings = {
  backendProtocol: string;
  backendAddress: string;
  backendPort: string;
};

// declare type SomeOtherSettings

declare type AppSettingsSliceType = BackendSettings;
